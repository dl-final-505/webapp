import styles from "../../styles/Home.module.css";

const Header = () => {
  return (
    <div className={styles.header}>
      <p className={styles.title}>Violapp</p>
    </div>
  );
};

export default Header;
